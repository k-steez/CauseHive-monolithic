{
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "python backend/monolithic_app/manage.py migrate --run-syncdb && python backend/monolithic_app/migrate_databases.py all && gunicorn --bind 0.0.0.0:$PORT --workers 3 --chdir backend/monolithic_app causehive_monolith.wsgi:application",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
